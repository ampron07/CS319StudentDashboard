# Student Study Planner & Grade Dashboard

## Run locally

```bash
npm install
npm run dev
```

Open the local Vite URL shown in the terminal. Courses, assignments, priority, and status are saved in `localStorage`, so a browser refresh keeps the current data.